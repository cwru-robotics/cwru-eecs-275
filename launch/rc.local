#!/bin/sh -e
#
#rc.local
# This script is executive at the end of each multiuser runlevel. 
# Make sure the script will "exit 0" on success or any other
# value on error. 
#
# In order to enable or disable this script just change the execution
# bits
#


sleep 30

sudo -i -u ubuntu /home/ubuntu/jetsonbot/devel/turtleStart.sh

exit 0
